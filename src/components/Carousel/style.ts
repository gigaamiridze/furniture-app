import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  carouselContainer: {
    flex: 1,
    alignItems: 'center',
  },
  imageComponentStyle: {
    width: '94%',
    borderRadius: 15,
  },
});
